#include <iostream>
using namespace std;

int main()
{
    int a, b;
    cout << "Enter length of rectangle: ";
    cin >> a;
    cout << "Enter breadth of rectangle: ";
    cin >> b;
    cout << "Perimeter of rectangle = " << 2 * (a + b) << " unit";

    return 0;
}